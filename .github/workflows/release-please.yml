on:
  push:
    branches:
      - master
name: ant-customize-form
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v3
        with:
          release-type: node
          package-name: ant-customize-form
